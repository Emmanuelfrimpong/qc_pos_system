enum Roles { admin, sales, mange }

enum ToastType { success, error, warning, info }

enum HomePageItemsList {
  home,
  user,
  sales,
  products,
  report,
  damages,
  settings,
  suppliers,
  customers,
  profile,

  newUser,
  newProduct,
  newSupplier,
  newCustomer,
  newDamage,
  newSale,
  editUser,
  editProduct,
  editSupplier,
  editCustomer,
  editDamage,
}

enum UserStatus { active, inactive, deleted, reset }
